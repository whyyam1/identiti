node_modules/
# Anchored to the repo root so it ignores Identiti's own build output
# (./dist) but NOT the vendored vendor/platform-shared/dist, which must be
# committed for Railway deploys.
/dist/
coverage/
.env
.env.local
# Generated RSA keypairs + the Railway staging variable block — never committed.
secrets/
*.log
*.tmp
.DS_Store
